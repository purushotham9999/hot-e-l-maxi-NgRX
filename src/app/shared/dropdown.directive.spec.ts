import { DropdownDirective } from './dropdown.directive';

xdescribe('DropdownDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new DropdownDirective();
  //   expect(directive).toBeTruthy();
  // });
});
